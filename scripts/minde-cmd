#!/bin/sh
# Compatibility alias for main-thread IPC evaluation.
set -eu

[ "$#" -eq 1 ] || { echo "usage: minde-cmd EXPR" >&2; exit 2; }
script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec "$script_dir/mindectl" eval "$1"
