#!/bin/sh
# Compatibility entry point. The Makefile owns the individual release gates
# so local development, Guix builds, and future hosted CI run identical checks.
set -eu
cd "$(dirname "$0")"
exec make check-all
