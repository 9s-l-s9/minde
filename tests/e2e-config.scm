;; SPDX-License-Identifier: GPL-3.0-or-later
;; The broad legacy behavior suite uses its historical Print bindings while
;; tests/portable-keymap-test.scm owns the shipped keymap contract.
(minde-config
 (version 1)
 (prefix () "Print")
 (bindings))
